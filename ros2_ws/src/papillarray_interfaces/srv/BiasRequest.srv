# ============================================================
# BiasRequest.srv — 偏置校准请求
#
# 向传感器发送偏置 (零位) 校准指令。
# 执行后传感器将当前各 pillar 的位移/力读数归零，
# 通常在无负载状态下调用，以消除零点漂移。
# ============================================================

# 上方为请求 (Request)，下方为响应 (Response)。ROS2 要求分隔符独占一行。
---

bool result   # 校准操作是否成功 (true = 成功)
