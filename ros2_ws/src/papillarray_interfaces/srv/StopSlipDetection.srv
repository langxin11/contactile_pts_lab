# ============================================================
# StopSlipDetection.srv — 停止滑动检测
#
# 关闭传感器内置的滑动检测算法。
# 停止后 slip_state 字段不再更新，
# 摩擦系数估计和目标抓取力也会停止计算。
# ============================================================

# 上方为请求，下方为响应。ROS2 要求分隔符独占一行。
---

bool result   # 停止操作是否成功
