# ============================================================
# StartSlipDetection.srv — 启动滑动检测
#
# 激活传感器内置的滑动检测算法。
# 激活时各 pillar 的当前接触状态被记录为参考基准，
# 后续数据中的 slip_state 字段将反映相对于该基准的变化。
# ============================================================

# 上方为请求，下方为响应。ROS2 要求分隔符独占一行。
---

bool result   # 启动操作是否成功
